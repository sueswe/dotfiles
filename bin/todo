#!/bin/bash

$(export | grep OS | grep -i windows)
rtc=$?
if [[ $rtc -eq 0 ]]; then
    #echo "Windows"
    #touch "$USERPROFILE"/Desktop/"$*"
    #touch $APHOMESHARE\\Desktop/"$*"
    touch $APDesktop/"$*"
else
    #echo "Unix"
    touch ~/Desktop/"$*"
fi


